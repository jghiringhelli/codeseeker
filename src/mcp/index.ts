/**
 * MCP (Model Context Protocol) Module
 *
 * Exposes CodeMind as an MCP server for integration with Claude Desktop
 * and Claude Code.
 */

export { CodeMindMcpServer, startMcpServer } from './mcp-server';