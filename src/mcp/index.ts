/**
 * MCP (Model Context Protocol) Module
 *
 * Exposes CodeSeeker as an MCP server for integration with Claude Desktop
 * and Claude Code.
 */

export { CodeSeekerMcpServer, startMcpServer } from './mcp-server';