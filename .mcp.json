{
  "mcpServers": {
    "codemind": {
      "command": "codemind",
      "args": ["serve", "--mcp"],
      "env": {
        "CODEMIND_STORAGE_MODE": "embedded"
      },
      "description": "CodeMind semantic search and code analysis MCP server"
    }
  }
}